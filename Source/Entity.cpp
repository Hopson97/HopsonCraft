#include "Entity.h"

Entity::Entity(const Vector3& rotation,
               const Vector3& position,
               const Vector3& scale)
:   rotation    (rotation)
,   position    (position)
,   scale       (scale)
{}
