#include "General_Maths.h"

#include "../World/World_Constants.h"

#include <cmath>

namespace Maths
{
/*
    Chunk::Position worldToChunkPos(const Vector3& position)
    {
        return  Chunk::Position(std::floor(position.x / World_Constants::CHUNK_SIZE),
                                std::floor(position.z / World_Constants::CHUNK_SIZE));
    }

    Block::Column_Position worldToBlockPos(const Vector3& position)
    {
        return  {(int32_t) position.x % World_Constants::CHUNK_SIZE,
                 (int32_t) position.y,
                 (int32_t) position.z % World_Constants::CHUNK_SIZE};
    }
*/
}
