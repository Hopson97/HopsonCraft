#include "Viewing_Frustum.h"
/*
namespace
{
    struct Plane
    {
        Vector3 normal;
        Vector3 point;
        float distance;

        void setPoints ( const Vector3& v1,
                         const Vector3& v2,
                         const Vector3& v3 );


    };
}

namespace Frustum
{
    namespace
    {
        Plane f_planes [6];
        enum
        {
            TOP = 0,
            BOTTOM,
            LEFT,
            RIGHT,
            NEARP,
            FARP
        };

        Vector3 ntl, ntr, nbl, nbr, ftl, ftr, fbl, fbr;
        float nearD, farD, ratio, angle, tang;
        float nw, nh, fw, fh;
    }

    struct Plane
    {
        Vector3 normal;
        Vector3 point;
        float distance;

        void setPoints ( const Vector3& v1,
                         const Vector3& v2,
                         const Vector3& v3 );


    };
}




*/













