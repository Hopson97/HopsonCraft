#include "CPosition.h"
/*
namespace Chunk
{
    Position::Position(int64_t x, int64_t y, int64_t z)
    :   x   (x)
    ,   y   (y)
    ,   z   (z)
    { }

    bool Position::operator ==(const Chunk::Position& other) const
    {
        return (x == other.x) && (y == other.y) && (z == other.z);
    }

    const Position& Position::operator-(const Chunk::Position& other) const
    {
        auto newX = x - other.x;
        auto newY = y - other.y;
        auto newZ = y - other.z;
    }
}
*/
