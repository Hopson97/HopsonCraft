#include "Body.h"
/*
void Body::addForce(const Vector3& force)
{
    m_velocity += force;
}

void Body::update(float dt)
{
    position += m_velocity * dt;

    onUpdate(dt);

    if (position.x < 0.2)
    {
        position.x = 0.2;
    }
    if (position.z < 0.2)
    {
        position.z = 0.2;
    }

    m_velocity *= 0.95;
}
*/
