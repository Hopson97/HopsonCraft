#include "Collisions.h"

#include "AABB.h"

namespace Collision
{
/*
    bool pointInAABB(const Vector3& point, const AABB& box)
    {
        return  (point.x >= box.getDim().x && point.x <= box.getPoint().x + box.getDim().x) &&
                (point.y >= box.getDim().y && point.y <= box.getPoint().y + box.getDim().y) &&
                (point.z >= box.getDim().z && point.z <= box.getPoint().z + box.getDim().z);

    }

    bool xAABB(const AABB& left, const AABB& right)
    {
        return  (left.getPoint().x + left.getDim().x >= right.getDim().x);
    }

    bool zAABB(const AABB& back, const AABB& front)
    {

    }

    bool yAABB(const AABB& top, const AABB& bottom)
    {

    }
*/
}
